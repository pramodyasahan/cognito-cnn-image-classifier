from tensorflow.keras.datasets import mnist    # This dataset has 70000 instances

(x_train, y_train), (x_test, y_test) = mnist.load_data()

print(x_train.shape, y_train.shape)
print(x_test.shape, y_test.shape)
